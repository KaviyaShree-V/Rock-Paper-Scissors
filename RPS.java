import java.util.Scanner;

public class RPS {
    public static void main(String[] args) { // Change method name to 'main'

        // Create an instance of RPSG (Rock Paper Scissors Game)
        RPSG game = new RPSG();
        // Start the game call the method startgame
        game.startGame();
    }
}
